---
'tongs-browser': minor
---

Add the transport for player-requested character sheets: a request that carries a correlation id, an
answer matched against it, and a timeout so a request nobody answers still ends. Not wired to a
button yet.
