---
'tongs-browser': patch
---

A synced encounter now sends its name, the scene players see it on and each combatant's initiative, so ComeOnOverUno can publish the fight to the campaign wiki. The GM can name an encounter with the `encounterName` flag; otherwise it takes the scene's name.
