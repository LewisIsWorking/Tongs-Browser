---
'tongs-browser': patch
---

Party campaigns: the list scrolls when a world has more parties than fit the window, and an empty field reads "No campaign" instead of a greyed code that looked saved.
